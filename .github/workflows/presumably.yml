name: blank

on:
  push:

jobs:
  ghost:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        beg: ["admit", "forget", "jump", "market", "input"]
        publish: ["reason", "raise", "temper", "nephew"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Wi
